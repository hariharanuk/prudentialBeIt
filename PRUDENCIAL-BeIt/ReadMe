Springboot-Survey-API for Prudential

Requirements
For building and running the application you need:

JDK 1.8
Maven 3

Running the application locally

There are several ways to run a Spring Boot application on your local machine. 
One way is to execute the main method in the org.springframework.boot.SpringApplication.PrudencialBeItApplication class from your IDE.